<tr>
  <td scope="row"><%= student.id %></td>
  <td><%= student.name %></td>
  <td><%= student.email %></td>
  <td><%= link_to "Edit", edit_student_path(student.id) %></td>
  <td><%= link_to "Delete", student_path(student.id) %></td>
</tr>

